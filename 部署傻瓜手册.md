# 星耀中国 · 万里同月 — 上线傻瓜手册（建站之星版）

## 一句话原理
活动网页需要一个**能跑后台程序**的服务器（它要收照片、记星光点、给总部打包下载）。
建站之星跑不了这种后台，但它能用「代码插件（iframe）」**嵌入一个外部网页**。

所以路线是：
> 用 **Render（免费）** 把活动网页托管起来 → 再用你们**建站之星的 iframe** 把它嵌进公司域名页面。
> 同事打开公司域名就能玩，照片/星光点自动收集，总部进后台下载做明信片。

全程你只需**点鼠标 + 复制两条命令**，约 15–20 分钟。

---

## 你来做（分 6 步）

### 第 1 步：注册 GitHub（没有才需要）
- 打开 https://github.com ，点 Sign up，用邮箱注册，记住用户名和密码。
- 有账号就跳过。

### 第 2 步：新建一个空仓库
- 登录后点右上角 **+ → New repository**
- Repository name 填：`xingyao-festival`
- 选 **Public**（Render 免费版需要公开仓库）
- **不要**勾任何初始化项，保持空仓库
- 点 **Create repository**

### 第 3 步：把代码推上去（二选一）

**方式 A（推荐小白）：用 GitHub Desktop 图形界面**
1. 下载安装 https://desktop.github.com/
2. 登录你的 GitHub
3. `File → Add Local Repository`，选这个文件夹：
   `C:/Users/PENNY YU/WorkBuddy/2026-08-14-15-25-46/wanlan-festival`
4. 点 **Publish repository**，名字填 `xingyao-festival`，勾 Public，发布

**方式 B：命令行（我已帮你备好本地仓库）**
在终端粘贴（把 `你的用户名` 换成你 GitHub 用户名）：
```bash
cd C:/Users/PENNY YU/WorkBuddy/2026-08-14-15-25-46/wanlan-festival
git remote add origin https://github.com/你的用户名/xingyao-festival.git
git branch -M main
git push -u origin main
```
> 密码不是登录密码，要用 Token：GitHub → Settings → Developer settings → Personal access tokens → 生成一个，复制粘贴当密码用。

### 第 4 步：Render 部署（约 5 分钟）
1. 打开 https://render.com ，用 GitHub 登录
2. **New → Web Service** → 选 `xingyao-festival` 仓库
3. 配置：
   - Build Command：**留空**（零依赖，不用安装）
   - Start Command：`npm start`
   - Instance Type：选最便宜的 **Free**
4. 展开 **Advanced → Add Environment Variable**，加两条：
   - `PORT` = `3000`
   - `ADMIN_TOKEN` = `一段只有你们知道的强口令，例如 Wanli2026@Moon`（**记好它**）
5. 点 **Create**，等约 1 分钟，得到 `https://xxx.onrender.com` 这个地址

### 第 5 步：建站之星嵌入（约 2 分钟）
1. 进建站之星后台 → 新建或编辑活动页面
2. 添加一个「**代码插件**」
3. 类型选 **iframe（iframe 标签嵌入外部网页）**
4. 地址填第 4 步拿到的：`https://xxx.onrender.com`
5. 保存并发布

### 第 6 步：测试一下
- 打开公司域名的活动页，传一张含月亮/国旗/月饼的照片，看地图是否点亮、灯谜是否出现
- 总部访问 `https://xxx.onrender.com/admin?token=你的强口令`，看能否打包下载图片 + 导出 CSV

---

## ⚠️ 两个必读提醒
1. **`ADMIN_TOKEN` 一定要设**——不设的话，任何人访问域名 + `/admin` 就能下载全员照片和留言。
2. **免费版磁盘重启会清空数据**——活动期间总部要**定期**进 `/admin` 下载照片和 CSV 备份；若想长期留存，可在 Render 挂 Persistent Disk（付费）或换公司自有服务器。

## 常见问题
- **嵌入后白屏？** 浏览器硬刷新 `Ctrl+Shift+R`；确认 iframe 地址是 `https://` 开头。
- **push 总失败？** 优先用方式 A 的 GitHub Desktop 图形界面，最省心。
- **想用公司自己的二级域名（如 `huodong.公司域名.com`）？** 在 Render 的 Settings → Custom Domains 绑定，需要改 DNS，可找公司 IT 协助。
- **网页改版了要更新？** 改完代码后 `git push` 一下，Render 会自动重新部署。

## 联调要点（给技术同事/IT 参考）
- 后端已加 `Content-Security-Policy: frame-ancestors *`，允许被任意外部网页（含建站之星 iframe）嵌入，无需额外配置。
- 服务监听 `process.env.PORT`（Render 自动注入），零第三方依赖，`npm start` 即启动。
- 数据目录 `data/`、`uploads/` 运行时自动创建；云端为临时磁盘，注意定期备份。
